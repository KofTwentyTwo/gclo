<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="gclo.PathRecoveryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:gclo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Repository has Windows-invalid paths"
    PrimaryButtonText="Apply and check out"
    CloseButtonText="Skip this repo"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick">

    <ContentDialog.Resources>
        <!-- Repository paths are wide; loosen the stock 548px dialog cap. -->
        <x:Double x:Key="ContentDialogMaxWidth">800</x:Double>
    </ContentDialog.Resources>

    <StackPanel MinWidth="480" Spacing="12">
        <TextBlock x:Name="IntroText" TextWrapping="Wrap" />
        <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="RowsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="local:PathRecoveryRow">
                        <Grid Margin="0,0,0,12" ColumnSpacing="12" RowSpacing="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                FontFamily="Consolas"
                                FontWeight="SemiBold"
                                IsTextSelectionEnabled="True"
                                TextWrapping="Wrap"
                                Text="{x:Bind RepoPath}" />
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                Style="{ThemeResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                TextWrapping="Wrap"
                                Text="{x:Bind Reason}" />
                            <TextBox
                                Grid.Row="2"
                                Grid.Column="0"
                                IsEnabled="{x:Bind RenameEnabled, Mode=OneWay}"
                                Text="{x:Bind NewName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <CheckBox
                                Grid.Row="2"
                                Grid.Column="1"
                                MinWidth="0"
                                VerticalAlignment="Center"
                                Content="Skip"
                                IsChecked="{x:Bind Skip, Mode=TwoWay}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</ContentDialog>
