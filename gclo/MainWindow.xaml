<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="gclo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:gclo"
    xmlns:vm="using:gclo.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="gclo">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Organization + token -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Header="GitHub organization"
                PlaceholderText="my-org"
                Text="{x:Bind ViewModel.Organization, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <PasswordBox
                Grid.Column="1"
                Header="Personal access token"
                PlaceholderText="ghp_..."
                PasswordChanged="TokenBox_PasswordChanged" />
        </Grid>

        <!-- Target folder + concurrency + actions -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Header="Target folder"
                PlaceholderText="C:\src\my-org"
                Text="{x:Bind ViewModel.TargetFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Grid.Column="1"
                VerticalAlignment="Bottom"
                Content="Browse..."
                Click="BrowseButton_Click" />
            <NumberBox
                Grid.Column="2"
                Header="Parallel"
                Minimum="1"
                Maximum="64"
                SpinButtonPlacementMode="Inline"
                Value="{x:Bind ViewModel.MaxConcurrencyValue, Mode=TwoWay}" />
            <Button
                Grid.Column="3"
                MinWidth="90"
                VerticalAlignment="Bottom"
                Style="{StaticResource AccentButtonStyle}"
                Content="Sync"
                Command="{x:Bind ViewModel.SyncCommand}" />
            <Button
                Grid.Column="4"
                MinWidth="90"
                VerticalAlignment="Bottom"
                Content="Cancel"
                Command="{x:Bind ViewModel.SyncCancelCommand}" />
        </Grid>

        <!-- Overall progress -->
        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="4">
                <ProgressBar
                    Maximum="{x:Bind ViewModel.TotalCount, Mode=OneWay}"
                    Value="{x:Bind ViewModel.CompletedCount, Mode=OneWay}" />
                <TextBlock
                    Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap" />
            </StackPanel>
            <TextBlock
                Grid.Column="1"
                VerticalAlignment="Top"
                Text="{x:Bind local:StatusFormat.Fraction(ViewModel.CompletedCount, ViewModel.TotalCount), Mode=OneWay}" />
        </Grid>

        <!-- Per-repo status list -->
        <ListView
            Grid.Row="3"
            ItemsSource="{x:Bind ViewModel.Repos}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:RepoItemViewModel">
                    <StackPanel Padding="0,6">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock
                                Text="{x:Bind StatusText, Mode=OneWay}"
                                Foreground="{x:Bind local:StatusFormat.BrushFor(Status), Mode=OneWay}" />
                        </StackPanel>
                        <TextBlock
                            Text="{x:Bind Error, Mode=OneWay}"
                            TextWrapping="Wrap"
                            Foreground="Crimson"
                            Visibility="{x:Bind local:StatusFormat.VisibleIf(HasError), Mode=OneWay}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
